(function () {
    angular
        .module("TipTag")
        .factory("TaskService", TaskService);
    
    function TaskService($http) {
        var api = {

        };
        return api;
    }
})();